---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dongggg                  # Ingress name
  labels:
    app: dongggg
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "dongggg.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dongggg               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
